services:
    postgres:
        image: 'postgres:latest'
        environment:
            - 'POSTGRES_DB=api-example'
            - 'POSTGRES_PASSWORD=secret'
            - 'POSTGRES_USER=api-example'
        ports:
            - '5432:5432'
